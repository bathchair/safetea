import React from 'react';
import { StyleSheet } from 'react-native';

import Colors from '../constants/Colors';
import { ExternalLink } from './ExternalLink';
import { MonoText } from './StyledText';
import { Text, View } from './Themed';

export default function NotificationItem({ data }: { key: string, data: object }) {
  return (
    <View>
        <View style={styles.itemContainer}>
            <Text>Hello</Text>
        </View>
    </View>
  );
}

const styles = StyleSheet.create({
  itemContainer: {
    maxWidth: "100%",
    maxHeight: 30,
    backgroundColor: "#fff"
    
  },
  homeScreenFilename: {
    marginVertical: 7,
  },
  codeHighlightContainer: {
    borderRadius: 3,
    paddingHorizontal: 4,
  },
  getStartedText: {
    fontSize: 17,
    lineHeight: 24,
    textAlign: 'center',
  },
  helpContainer: {
    marginTop: 15,
    marginHorizontal: 20,
    alignItems: 'center',
  },
  helpLink: {
    paddingVertical: 15,
  },
  helpLinkText: {
    textAlign: 'center',
  },
});
